export const metadata = {
    title: '다르크협회 외부채널',
    description: '다르크 협회의 유튜브, 인스타그램, 텔레그램 등 다양한 외부 채널을 확인하세요.',
};

export const viewport = {
    width: 'device-width',
    initialScale: 1,
};

export default function ResourcesLayout({ children }) {
    return <>{children}</>;
}